export function smoothScroll(e){
    e.stopPropagation();
    // e.preventDefault();

    // let currentPosition = window.pageYOffset;
    // let target = document.getElementById('quien-soy').offsetTop;

    // setTimeout(() => {
    //     window.scrollTo(currentPosition, target + currentPosition);
    // }, 500);
}